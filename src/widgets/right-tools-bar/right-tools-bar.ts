import UIComponent from "@/shared/components/ui-component";

export class RightToolsBar extends UIComponent {
	constructor(id: string, blockType = "aside") {
		super(id, blockType);
	}

	addContent(): void {}
}
